---
version: 0.6.0
date: 2026-09-06
---

# Theatre 0.6.0

Agents can switch Stage between Godot projects without restarting their MCP
session, and inspect disabled physics areas without producing engine errors.

## Delivered outcomes

- **switch-stage-project-within-mcp-session:** `project_select` selects one project
  explicitly and discards watches, baselines, spatial indexes, session overrides
  and cached clip location, including when selecting the same project. It loads
  the new defaults and returns selected path/port separately from verified runtime
  identity and readiness. Invalid inputs preserve the previous selection;
  unavailable targets remain selected and retry without fallback. Whole MCP calls
  drain before old connection teardown completes, and an accepted transition
  finishes even if its requesting call is cancelled. Tool descriptions, response
  messages and distributed agent guidance explain the fresh-snapshot requirement.
- **stage-inspects-disabled-area-monitoring:** Area2D/Area3D child inspection and
  the 3D containing-area scan check monitoring before querying overlaps. Disabled
  overlap data is null, not an observed empty list. Inspection never enables
  monitoring, and traversal continues below disabled areas.

## Operating boundary

Director continues to select `project_path` per call. Stage selection does not
start or stop games, delete saved recordings, restore previous sessions, discover
ports automatically, or change the external client feedback hook's environment.
Its explicit port overrides the selected project's TOML port; absent both it uses
9077 rather than retaining the old target's startup port. Existing MCP processes
must restart once to load this release; subsequent target changes use the tool.
Deploy the matching addon and restart affected Godot processes to load the
monitoring correction. Godot 4.7 remains the extension API target.

## Verification

Workspace build, formatting, Rust 1.98 warnings-denied Clippy, ordinary workspace
tests and the complete explicitly invoked engine suite passed on Linux/Godot
4.7.1. The affected Stage engine suite passed again against the reviewed final
implementation, including real A-to-B-to-A and same-target switching. Site schema
generation/build, bundled-skill synchronization and release-script checks passed.

The original monitoring error was reproduced in all three affected collector
paths before correction. A multithreaded negative control for old-task teardown
failed without joining; the corrected race/cancellation regression passed.
One fresh-context Astra high reviewer reviewed both implementations, then
rechecked the material teardown correction with no unresolved material or blocking
findings. No additional Workbench release gates are configured.

Fresh isolated editor import on this machine separately aborted before an MCP
connection. The runtime-switch test uses generated discovery metadata from the
already-imported fixture; it does not claim an import crash fix. See
`../backlog/headless-editor-import-shutdown-abort.md`. The previously recorded
intermittent native editor accessibility crash also remains unclaimed as fixed;
a passing engine suite is not evidence of its repair. Windows/macOS runtime
behavior has not been qualified locally.

## Publication and rollout

The [release workflow](https://github.com/nklisch/theatre/actions/runs/34020928495),
[cross-platform CI](https://github.com/nklisch/theatre/actions/runs/34020927555) and
[documentation publication](https://github.com/nklisch/theatre/actions/runs/34020927597)
passed. The public release includes Linux, Windows and Apple Silicon bundles.

The installed Theatre CLI, Stage and Director all report 0.6.0. The installed
MCP binary exposes 14 Stage tools, including project_select's explicit reset
warning. Both regression journeys passed again with the release addon.

Matched Stage, Director and shared-support payloads were deployed to all 32
configured Voxlar sandbox copies across 13 non-archival checkouts. All 2,048
installed payload files matched the installed templates/binary, and pre-existing
project.godot, stage.toml and nested MCP configuration contents were unchanged.
Recording-disabled startup was preserved. Approved feedback/shared-support ignore
rules and existing vendored operating guidance were refreshed; the owned setup
changes were committed locally in each affected Voxlar checkout, without pushing
Voxlar or including unrelated local work.

Historical performance-reference source trees and addon-free examples were left
untouched. Already-running Godot/MCP processes were not stopped or claimed to have
reloaded the replacement binaries; they need a restart to use this release.
